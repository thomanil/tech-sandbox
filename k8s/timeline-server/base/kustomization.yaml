# Declares which manifests make up the base. Overlays reference this directory
# (resources: [../../base]) and layer patches + extra resources on top.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
